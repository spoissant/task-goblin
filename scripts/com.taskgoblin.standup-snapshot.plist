<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!-- Runs `bun run standup:snapshot` to take the nightly snapshot.
     Install:  cp scripts/com.taskgoblin.standup-snapshot.plist ~/Library/LaunchAgents/
               launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.taskgoblin.standup-snapshot.plist
     Remove:   launchctl bootout gui/$(id -u)/com.taskgoblin.standup-snapshot -->
<plist version="1.0">
<dict>
  <key>Label</key>
  <string>com.taskgoblin.standup-snapshot</string>

  <!-- A login shell so bun, asdf shims and $PATH resolve the same way they do
       in a terminal. launchd itself has almost no PATH. -->
  <key>ProgramArguments</key>
  <array>
    <string>/bin/zsh</string>
    <string>-lc</string>
    <string>cd /Users/simonpoissant/Code/task-goblin &amp;&amp; bun run standup:snapshot</string>
  </array>

  <key>StartCalendarInterval</key>
  <dict>
    <key>Hour</key><integer>0</integer>
    <key>Minute</key><integer>5</integer>
  </dict>

  <key>RunAtLoad</key>
  <false/>

  <key>StandardOutPath</key>
  <string>/Users/simonpoissant/Code/task-goblin/logs/standup-snapshot.log</string>
  <key>StandardErrorPath</key>
  <string>/Users/simonpoissant/Code/task-goblin/logs/standup-snapshot.log</string>
</dict>
</plist>
